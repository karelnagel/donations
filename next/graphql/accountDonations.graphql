query accountDonations($owner: String = "") {
  donations(where: { owner_contains_nocase: $owner }, orderBy: time, orderDirection: desc) {
    id
    message
    amount
    owner 
    project {
      coin
      index
      name
      image
      collection {
        id
      }
    }
    time
  }
}
