query donation($id: ID = "") {
  donation(id: $id) {
    amount
    donator {
      id
    }
    message
    time
    collection {
      name
      description
      url
      coin
      image
      socials
      donationOptions
      goal
    }
  }
}
