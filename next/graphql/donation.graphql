query donation($id: ID = "") {
  donation(id: $id) {
    amount
    donator {
      id
    }
    message
    time
    collection {
      name
      description
      url
      coin{id}
      image
      socials
      donationOptions
      goal
    }
  }
}
