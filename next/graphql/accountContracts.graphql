query accountContracts($owner: String = "") {
  contracts(where: { owner_contains_nocase: $owner }, orderBy: time, orderDirection: desc) {
    id
    address
    time
    owner {
      id
    }
  }
}
