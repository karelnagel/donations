query project($id: ID = "") {
  project(id: $id) {
    count
    active
    coin
    time
    id
    owner {
      id
    }
    contract {
      id
      address
      owner {
        id
      }
    }
  }
}
