query latestProjects($first: Int = 10) {
  projects(first: $first, orderBy: time, orderDirection: desc) {
    donated
    time
    count
    coin
    active
    contract {
      id
    }
    owner {
      id
    }
  }
}