specVersion: 0.0.2
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum
    name: Factory
    network: {{network}}
    source:
      address: "{{address}}"
      abi: Factory
      startBlock: {{startBlock}}
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.6
      language: wasm/assemblyscript
      entities:
        - Collection
      abis:
        - name: Factory
          file: ./abis/contracts/factory.sol/Factory.json
      eventHandlers:
        - event: NewCollection(string,address,address,address,address,string)
          handler: handleNewCollection
        - event: SetURI(string)
          handler: handleSetURI
      file: ./src/mappings.ts

templates:
  - name: Collection
    kind: ethereum
    network: {{network}}
    source:
      abi: Collection
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.6
      language: wasm/assemblyscript
      file: ./src/mappings.ts
      entities:
        - Collection
        - Project
        - Donation
      abis:
        - name: Collection
          file: ./abis/contracts/Collection.sol/Collection.json
      eventHandlers:
        - event: NewProject(uint256,address,address,string)
          handler: handleNewProject
        - event: NewDonation(uint256,uint256,address,uint256,string)
          handler: handleNewDonation
        - event: End(uint256)
          handler: handleEnd
        - event: SetIPFS(uint256,string)
          handler: handleSetIPFS
        - event: Transfer(indexed address,indexed address,indexed uint256)
          handler: handleTransfer
        - event: OwnershipTransferred(indexed address,indexed address)
          handler: handleOwnershipTransferred
