
export function getSupporterId(title: string, address: string): string { return title + "_a" + address }
export function getDonationId(title: string, tokenId: string): string { return title + "_t" + tokenId }
