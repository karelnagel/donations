type Token @entity {
  id: ID!
  title: String!
  token: Bytes! 
  coin: Bytes! 
  owner: Bytes! 
  contractURI: String! 
  currentProject: Int!
  projects: [Project!]! @derivedFrom(field: "token")
  active: Boolean!
  donated:BigInt!
  withdrawn:BigInt!
  time:BigInt!
}

type Donation @entity {
  id: ID!
  amount: BigInt!
  message: String!
  sender: Bytes!
  project: Project!
  time:BigInt!
}

type Project @entity{
  id: ID!
  order:Int!
  image:String!
  token:Token!
  time: BigInt!
  donated:BigInt!
  donations: [Donation!]! @derivedFrom(field: "project")
}

